# Los scripts de shell necesitan finales de línea LF aunque se clonen en
# Windows: con CRLF, bash falla con un críptico $'\\r': command not found.
*.sh text eol=lf

# Los .cmd, al contrario, quieren CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
